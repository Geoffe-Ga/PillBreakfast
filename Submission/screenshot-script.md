# Screenshot Script

Step-by-step recipe for producing the App Store Connect screenshot set on a
paired iPhone + watch simulator: which seeded data to load, which screen to
capture on each device, the exact tap sequence to reach each capture point,
and the export settings. Filled in by the screenshot-script issue in EPIC 10.
