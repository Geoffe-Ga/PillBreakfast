#!/usr/bin/env bash
# Stub for the mutation-testing harness — filled in by the mutation issue in
# EPIC 10. Returns 0 so the loop's CI doesn't trip over its absence.
set -euo pipefail
echo "scripts/mutate.sh — stub (fill in EPIC_10_ISSUE_06)"
exit 0
